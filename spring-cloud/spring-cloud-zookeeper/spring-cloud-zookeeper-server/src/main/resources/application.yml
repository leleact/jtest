spring:
  cloud:
    zookeeper:
      connect-string: localhost:2189,localhost:2181,localhost:2183
      discovery:
        enabled: true
        preferIpAddress: true
  application:
    name: zookeeper-server
  profiles:
    active: s1